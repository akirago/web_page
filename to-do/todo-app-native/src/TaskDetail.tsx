import React from 'react';
import {
  ActivityIndicator,
  StyleSheet,
  Button,
  Alert,
  Text,
  View,
} from 'react-native'
import CustomButton from './CustomButton';

const TaskDetail = () => {
    return(
      <View>
        <Text>タスク詳細画面</Text>
      </View>
    );
};

export default TaskDetail;